using Point = {mutable float x, mutable float y};
Point a = {1, 2};
Point b = {5, 6};

float some_func()
{
    a = b;
    return 3;
}

float c = a.x * b.y;
float _ = some_func();
float d = b.x * a.y;

using Point = {float x, float y};
mutable Point b = {5, 6};

float some_func()
{
    b[0] = 3;
    b.x = b[1];
    b[1] = 7;
    return 0;
}

float c = b.x * b.y;
float z = some_func();
float d = b.x * b.y + z;

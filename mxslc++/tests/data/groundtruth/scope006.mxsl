using Point = {float x, float y};
mutable Point a = {1, 2};
mutable Point b = {5, 6};

float some_func()
{
    a = {3, 4};
    b = {7, 8};
    return 3;
}

float c = a.x * b.y;
float _ = some_func();
float d = b.x * a.y;

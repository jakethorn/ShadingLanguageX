inline void foo(float a, vec3 b, string c)
{
    surfaceshader surface = standard_surface();
    surface.base = a;
    surface.base_color = color3{b};
    surface.normal = normal(space=c);
    surface.thin_walled = false;

    surfacematerial(surface);
}
